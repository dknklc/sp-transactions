server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/transaction
    username: root
    password: secret
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    org:
      springframework:
        transaction:
          interceptor: TRACE